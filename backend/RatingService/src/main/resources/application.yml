spring:
  data:
    mongodb:
      uri: mongodb+srv://garima:garima123@cluster0.lwdpo4r.mongodb.net/
      database: microservices
  application:
    name: RATING-SERVICE
server:
  port: 8083


eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
